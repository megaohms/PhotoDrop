var config = {};

//Set url for API
  // To build on simulator, set url to 'localhost'
  // To build on an external iPhone, set url to your IP address. System Preferences > Nextwork > Advanced Settings > TCP/IP > XX.X.XX.XXX

config.url = '10.6.30.148'

module.exports = config;