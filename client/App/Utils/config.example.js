var config = {};

//Set url for API
  // First, rename this file to 'config.js'. It will be ignored by git
  // Then, set the url below
  // To build on simulator, set to 'localhost'
  // To build on an external iPhone, set to your IP address. System Preferences > Nextwork > Advanced Settings > TCP/IP > XX.X.XX.XXX

config.url = 'localhost'

module.exports = config;